# k8s/node-backend/backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: node-backend-service
  namespace: iot-system
spec:
  selector:
    app: node-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000