export const environment = {
  production: true,
  apiUrl: '',
  apiV2Url: '',         // empty → derived at runtime as api.{hostname} (new `api` service, F2)
  deviceGatewayUrl: '', // empty → derived at runtime as device.{hostname}
  socketUrl: '',        // empty → derived at runtime as socket.{hostname}
  googleClientId: '670668044114-iing0d4h94kd2gno8fb2m1mbilkeflu9.apps.googleusercontent.com',
};