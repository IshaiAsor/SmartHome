{
  "name": "lattice",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*",
    "tools/*"
  ],
  "scripts": {
    "build:libs": "npm run build -w @lattice/logger && npm run build -w @lattice/otel && npm run build -w @lattice/queue && npm run build -w @lattice/jwt && npm run build -w @lattice/prisma-client && npm run build -w @lattice/ml && npm run build -w @lattice/ioredis",
    "test": "jest",
    "test:e2e": "jest tests/e2e"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5"
  },
  "engines": {
    "node": ">=20"
  }
}
