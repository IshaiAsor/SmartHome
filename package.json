{
  "name": "lattice",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build:libs": "npm run build -w @lattice/logger && npm run build -w @lattice/otel && npm run build -w @lattice/queue && npm run build -w @lattice/jwt && npm run build -w @lattice/prisma-client"
  },
  "engines": {
    "node": ">=20"
  }
}
